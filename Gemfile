source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in simple_ssh.gemspec
gemspec

gem 'colorize'

group :development do
  gem 'rspec'
end
